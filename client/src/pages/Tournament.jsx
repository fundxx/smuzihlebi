import { Box, Container } from "@mui/material";
import React from "react";

const Tournament = () => {
    return (
        <Container>
            <Box
                sx={{
                    display: "flex",
                    alignItems: "center",
                    padding: "20px",
                }}
            ></Box>
        </Container>
    );
};

export default Tournament;
